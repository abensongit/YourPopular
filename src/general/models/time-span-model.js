/**
 * Trending 搜索时间跨度
 * @param showText
 * @param searchText
 * @constructor
 */
export default function TimeSpanModel(showText, searchText) {
  this.showText = showText;
  this.searchText = searchText;
}
